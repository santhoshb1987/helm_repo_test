apiVersion: v1
kind: Service
metadata:
  name: {{ include "repotest2.fullname" . }}
  labels:
    {{- include "repotest2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "repotest2.selectorLabels" . | nindent 4 }}
