apiVersion: v1
kind: Service
metadata:
  name: {{ include "santhoshtest.fullname" . }}
  labels:
    {{- include "santhoshtest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "santhoshtest.selectorLabels" . | nindent 4 }}
